apiVersion: v1
kind: Service
metadata:
  name: tradewisenexus-backend-service
spec:
  type: NodePort
  selector:
    app: tradewisenexus-backend
  ports:
    - port: 8087
      targetPort: 8087
      nodePort: 30087
