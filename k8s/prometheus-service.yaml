apiVersion: v1
kind: Service
metadata:
  name: tradewisenexus-prometheus-service
spec:
  type: NodePort
  selector:
    app: tradewisenexus-prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30092
