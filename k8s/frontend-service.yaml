apiVersion: v1
kind: Service
metadata:
  name: tradewisenexus-frontend-service
spec:
  type: NodePort
  selector:
    app: tradewisenexus-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30100
