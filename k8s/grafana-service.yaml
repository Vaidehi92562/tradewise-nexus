apiVersion: v1
kind: Service
metadata:
  name: tradewisenexus-grafana-service
spec:
  type: NodePort
  selector:
    app: tradewisenexus-grafana
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30030
